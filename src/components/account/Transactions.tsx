import * as React from 'react';
import * as styles from './styles/Transactions.scss';

const Transactions: React.SFC<{}> = (props) => (
    <div
        className={styles.transactions}
    >

    </div>
);

export default Transactions;

